# Docker environment template — safe to commit, no real secrets here.
# Copy to .env and fill in real values if running outside docker-compose.
#
# When using docker-compose, these are set directly in docker-compose.yml.
# The DB_URL uses "db" as the hostname (the docker-compose service name).

DB_URL=postgres://chirpy:dev@db:5432/chirpy?sslmode=disable
PLATFORM=dev
JWT_SECRET=change-me-for-production
POLKA_KEY=f271c81ff7084ee5b99a5091b42d486e
CORS_ORIGIN=http://localhost:8888
PORT=8888
